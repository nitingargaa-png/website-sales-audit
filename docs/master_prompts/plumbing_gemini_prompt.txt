================================================================================
PLUMBING — GEMINI 3.1 PRO PREVIEW CINEMATIC PROMPT
Optimized for: Google AI Studio | Gemini 3.1 Pro Preview
Thinking level: HIGH (set before submitting)
Output: Complete multi-file React/Vite/Tailwind project — ALL files, no truncation
Generated by: docs/master_prompts/plumbing_gemini_prompt.txt

HOW TO USE:
1. Go to aistudio.google.com
2. Select Gemini 3.1 Pro Preview
3. Set thinking level to HIGH (top right of prompt area)
4. Paste this entire prompt
5. Gemini outputs complete file tree — download all files
6. npm install && npm run build
7. netlify deploy --dir=dist --prod

OR with Antigravity CLI:
  antigravity generate --prompt plumbing_gemini_prompt.txt --output ./client-site

Replace [BRACKETED_VARIABLES] before pasting.
================================================================================

## ROLE

You are a senior front-end engineer building a premium, cinematic marketing website
for a local plumbing business. This is not a template — this is a bespoke,
high-converting website that should look like it cost $10,000 to build.

Design reference: Think Stripe, Linear, or Vercel's marketing pages — but adapted
for a trades business. Clean, confident, fast, with purposeful motion.

---

## BUSINESS DATA

```
Business Name:    [BUSINESS_NAME]
Phone:            [PHONE]
Phone (digits):   [PHONE_DIGITS]   ← used in all href="tel:+1[PHONE_DIGITS]"
City/Province:    [CITY_PROVINCE]
Services:         [SERVICES_LIST]
Service Areas:    [SERVICE_AREAS]
Years in business:[YEARS_IN_BUSINESS]
Google rating:    [RATING_STRING]
```

Customer reviews (display verbatim — do not paraphrase):
```
[REVIEWS_3]
```

If any variable is empty, use intelligent fallback copy appropriate for an
emergency plumbing business in [CITY_PROVINCE].

---

## OUTPUT FORMAT — CRITICAL

Output EVERY file completely. Do not truncate, summarize, or use comments like
"// rest of component here" or "// ... similar to above". Gemini's 64k token
output handles this — use it fully.

Output each file with a clear header:

```
=== FILE: [filename] ===
[complete file content]
=== END FILE ===
```

Files to output:
1. `package.json`
2. `vite.config.js`
3. `tailwind.config.js`
4. `postcss.config.js`
5. `index.html`
6. `src/index.css`
7. `src/main.jsx`
8. `src/App.jsx`
9. `src/components/Header.jsx`
10. `src/components/Hero.jsx`
11. `src/components/VoiceWidgetPlaceholder.jsx`
12. `src/components/TrustBar.jsx`
13. `src/components/Services.jsx`
14. `src/components/WhyChooseUs.jsx`
15. `src/components/MidPageCTA.jsx`
16. `src/components/Process.jsx`
17. `src/components/Reviews.jsx`
18. `src/components/ServiceAreas.jsx`
19. `src/components/Booking.jsx`
20. `src/components/Contact.jsx`
21. `src/components/Footer.jsx`
22. `public/favicon.svg`

---

## DESIGN SYSTEM

### Color Palette
```css
--color-navy:        #0f1f3d;   /* deep navy — primary backgrounds */
--color-navy-mid:    #1a3a6b;   /* mid navy — headers, cards */
--color-navy-light:  #1e4a8a;   /* lighter navy — hover states */
--color-emergency:   #dc2626;   /* emergency red — primary CTA, urgent elements */
--color-emergency-h: #b91c1c;   /* red hover */
--color-accent:      #3b82f6;   /* blue — secondary accents, links */
--color-gold:        #f59e0b;   /* gold — star ratings, trust signals */
--color-white:       #ffffff;
--color-off-white:   #f8fafc;
--color-gray-100:    #f1f5f9;
--color-gray-200:    #e2e8f0;
--color-gray-500:    #64748b;
--color-text:        #0f172a;   /* near-black body text */
```

### Typography
- Headlines: font-black, tight leading (leading-[0.95])
- Subheadlines: font-semibold, relaxed leading
- Body: font-normal, leading-relaxed
- CTAs: font-bold, tracking-wide
- Import: Use system font stack — no Google Fonts (performance)

### Spacing Scale
Use Tailwind's default scale. Section padding: py-20 (desktop) py-12 (mobile).
Generous whitespace — this signals premium quality.

### Animation Philosophy
- Animations must feel earned — not decorative noise
- Entry animations: fade up + slight scale (0.96 → 1.0)
- Stagger children in grids: 100ms delay per item
- Hover: scale(1.02) + subtle shadow lift — 200ms ease
- Scrolled-past sections: don't re-animate
- Respect prefers-reduced-motion

---

## CINEMATIC FEATURES (use all of these — this is why you're using Gemini)

### 1. CSS Keyframe Animations

Define in `src/index.css`:

```css
@keyframes fadeUpIn {
  from { opacity: 0; transform: translateY(28px) scale(0.97); }
  to   { opacity: 1; transform: translateY(0)    scale(1);    }
}

@keyframes fadeIn {
  from { opacity: 0; }
  to   { opacity: 1; }
}

@keyframes slideInLeft {
  from { opacity: 0; transform: translateX(-32px); }
  to   { opacity: 1; transform: translateX(0); }
}

@keyframes slideInRight {
  from { opacity: 0; transform: translateX(32px); }
  to   { opacity: 1; transform: translateX(0); }
}

@keyframes pulseGlow {
  0%, 100% { box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.4); }
  50%       { box-shadow: 0 0 0 12px rgba(220, 38, 38, 0); }
}

@keyframes countUp {
  from { opacity: 0; transform: translateY(8px); }
  to   { opacity: 1; transform: translateY(0); }
}

/* Reduced motion override */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

### 2. IntersectionObserver Scroll Reveals

Create a reusable `useScrollReveal` hook in `src/hooks/useScrollReveal.js`:

```js
import { useEffect, useRef, useState } from 'react';

export function useScrollReveal(options = {}) {
  const ref = useRef(null);
  const [isVisible, setIsVisible] = useState(false);

  useEffect(() => {
    const el = ref.current;
    if (!el) return;
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          setIsVisible(true);
          observer.unobserve(el); // only trigger once
        }
      },
      { threshold: 0.12, rootMargin: '0px 0px -40px 0px', ...options }
    );
    observer.observe(el);
    return () => observer.disconnect();
  }, []);

  return [ref, isVisible];
}
```

Apply to every section component:
```jsx
const [ref, isVisible] = useScrollReveal();
// On section wrapper:
style={{
  animation: isVisible ? 'fadeUpIn 0.6s ease forwards' : 'none',
  opacity: isVisible ? 1 : 0
}}
```

Stagger grid children:
```jsx
style={{
  animation: isVisible ? `fadeUpIn 0.5s ease ${index * 0.1}s forwards` : 'none',
  opacity: 0
}}
```

### 3. Hero Parallax

In `Hero.jsx`, add a subtle parallax on the background:

```jsx
useEffect(() => {
  const handleScroll = () => {
    const y = window.scrollY;
    if (bgRef.current) {
      bgRef.current.style.transform = `translateY(${y * 0.3}px)`;
    }
  };
  window.addEventListener('scroll', handleScroll, { passive: true });
  return () => window.removeEventListener('scroll', handleScroll);
}, []);
```

### 4. Glassmorphism Trust Bar

The trust bar cards use a frosted glass effect:

```css
.trust-card {
  background: rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(255, 255, 255, 0.15);
  border-radius: 12px;
}
```

Place the trust bar directly below the hero (still on the navy background) so the
glassmorphism effect reads correctly against a dark background.

### 5. Emergency CTA Pulse

The primary "Call Now" button has a continuous pulse glow:

```css
.btn-emergency {
  animation: pulseGlow 2s ease-in-out infinite;
}
.btn-emergency:hover {
  animation: none; /* stop pulse on hover, use scale instead */
  transform: scale(1.04);
}
```

### 6. Service Card Hover

Each service card:
```css
.service-card {
  transition: transform 200ms ease, box-shadow 200ms ease;
}
.service-card:hover {
  transform: translateY(-4px) scale(1.015);
  box-shadow: 0 20px 40px rgba(15, 31, 61, 0.15);
}
```

### 7. Counter Animation (optional — use if RATING_STRING has a number)

Animate the review count in the hero subheadline from 0 to the real number
using a short 1.2s ease-out animation on page load.

### 8. Gradient Text

Use for key headlines where appropriate:
```css
.gradient-text {
  background: linear-gradient(135deg, #ffffff 0%, #93c5fd 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
```

---

## SECTION-BY-SECTION SPECIFICATIONS

### vite.config.js (CRITICAL)
```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  build: {
    outDir: 'dist',  // explicit — required for netlify deploy --dir=dist --prod
  }
})
```

### index.html
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>[BUSINESS_NAME] | Plumber in [CITY_PROVINCE]</title>
  <meta name="description" content="[BUSINESS_NAME] — trusted plumber in [CITY_PROVINCE].
    [RATING_STRING]. Emergency plumbing available 24/7. Call [PHONE].">
  <meta property="og:title" content="[BUSINESS_NAME] | [CITY_PROVINCE] Plumber">
  <meta property="og:description" content="[RATING_STRING]. Emergency plumbing,
    drain cleaning, water heater repair. Serving [SERVICE_AREAS].">
  <meta property="og:type" content="website">
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
</head>
<body>
  <!-- GHL INLINE VOICE WIDGET — static HTML outside React root
       Place here so div exists before React mounts.
       Replace with actual GHL snippet after setup. -->
  <div id="ghl-voice-inline" style="display:none;"></div>

  <div id="root"></div>
  <script type="module" src="/src/main.jsx"></script>

  <!-- GHL FLOATING CHAT WIDGET — always before </body> -->
  <div id="ghl-chat-widget"></div>
  <!-- GHL: paste floating chat/voice widget JS snippet here -->
</body>
</html>
```

**Note on ghl-voice-inline placement:** It is placed as static HTML OUTSIDE
`<div id="root">` so it exists in the DOM before React hydrates. The VoiceWidgetPlaceholder
component in React renders a visual placeholder in the correct layout position —
after GHL setup, the real widget populates the static div above.

### Header.jsx

Sticky, full-width. Transitions from transparent (on hero) to white with shadow
(on scroll). Uses a `useEffect` + `scroll` listener to toggle the scrolled state.

Desktop: Logo left | nav links center | phone CTA button right + missed-call badge.
Mobile: Logo left | phone CTA button right only. No hamburger menu needed.

Phone button has `pulseGlow` animation (subtle, not distracting).
Below desktop nav: small italic text in navy/gray: "Missed us? We'll text you back in 60 seconds."

Nav links smooth-scroll to section IDs: #services, #service-areas, #reviews, #contact.

### Hero.jsx

Full-viewport-height section. Navy gradient background with a subtle diagonal
grid pattern overlay (CSS only, no images needed):

```css
background: linear-gradient(135deg, #0f1f3d 0%, #1a3a6b 60%, #1e3a8a 100%);
```

Grid pattern overlay:
```css
background-image:
  linear-gradient(rgba(255,255,255,0.03) 1px, transparent 1px),
  linear-gradient(90deg, rgba(255,255,255,0.03) 1px, transparent 1px);
background-size: 60px 60px;
```

Content layout (desktop: 60% left / 40% right stats panel):

LEFT COLUMN:
- Small eyebrow tag: "⚡ Emergency Plumbing • [CITY_PROVINCE]" — red pill badge
- H1 (massive, white, font-black):
  "Plumbing Emergency in [CITY_PROVINCE]?"
  Second line (gradient text): "We Arrive in 60 Minutes."
- Subheadline: "⭐⭐⭐⭐⭐ [RATING_STRING] • Trusted by [CITY_PROVINCE] homeowners"
- Two CTA buttons in a row:
  Button 1 (red, large, pulse glow): "📞 Call Now — 24/7"  href="tel:+1[PHONE_DIGITS]"
  Button 2 (outlined white, ghost): "Get a Free Quote"  scrolls to #contact
- Trust micro-badges below buttons:
  "🔒 Licensed & Insured  •  ⚡ Available 24/7  •  📋 Free Estimates"

RIGHT COLUMN (glassmorphism panel, desktop only):
Show 3 stat cards with large numbers + labels:
  "[YEARS_IN_BUSINESS]+" / "Years Serving [CITY_PROVINCE]"
  "[review count from RATING_STRING]" / "5-Star Google Reviews"
  "60 min" / "Average Response Time"
Each card: glassmorphism background (rgba white + blur), gold accent on number.

Parallax: background grid moves at 0.3x scroll speed (see parallax spec above).

Hero entrance animation: left column fades up (0.7s), right column slides in from
right (0.6s, 0.2s delay). Both triggered on mount, not on scroll.

### VoiceWidgetPlaceholder.jsx

Renders a styled placeholder in the visual layout (between hero and trust bar):

```jsx
export default function VoiceWidgetPlaceholder() {
  return (
    <div className="w-full py-4 px-6 bg-gray-50">
      <div
        style={{
          border: '2px dashed #cbd5e1',
          borderRadius: '8px',
          minHeight: '80px',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          padding: '20px',
          color: '#94a3b8',
          textAlign: 'center',
        }}
      >
        🎙️ Voice AI Widget — activates after GHL setup
      </div>
    </div>
  );
}
```

The actual GHL inline widget JS will populate `id="ghl-voice-inline"` (the static
HTML element in index.html). This component is just the visual placeholder.

### TrustBar.jsx

Placed immediately after the voice widget placeholder, still within the navy
hero section visually — use a dark navy background so glassmorphism reads well.

4 cards in a horizontal row (desktop) / 2×2 grid (mobile):
```
⚡ Available 24/7      | "Emergency response any time"
✅ Licensed & Insured  | "Fully certified & bonded"
⏱️ 60 Min Response    | "Fast dispatch, every call"
📋 Free Estimates      | "Know the price before we start"
```

Each card: glassmorphism (see spec above), icon large + bold title + small subtitle.
Scroll reveal with 100ms stagger between cards.

### Services.jsx

Section ID: `#services`
Light off-white background (#f8fafc).
Heading: "Our Plumbing Services in [CITY_PROVINCE]"
Subheading: "From emergency callouts to scheduled maintenance — we handle everything."

Card grid: 3 columns desktop, 2 tablet, 1 mobile.
Use services from [SERVICES_LIST]. Fallback if empty:
  Emergency Plumbing, Drain Cleaning, Water Heater Repair, Pipe Repair,
  Toilet Installation, Faucet Repair, Sump Pump Installation, Sewer Line Repair,
  Backflow Prevention, Bathroom Plumbing, Kitchen Plumbing, Leak Detection

Each card:
- White background, subtle border (#e2e8f0), border-radius: 12px
- Top: colored icon container (navy bg, white icon emoji — use appropriate plumbing emojis)
- Service name: font-semibold text-navy
- 1-line description: text-gray-500 text-sm
- Emergency services (first 2): red left border (border-l-4 border-red-500)
- Hover: translateY(-4px) + shadow lift (see spec)

Scroll reveal with stagger: each card reveals 100ms after the previous.

### WhyChooseUs.jsx

3-column layout, white background.
No heading — let the three differentiators speak for themselves.
Each column: large icon (64px, in a circle bg) + bold heading + 2-sentence paragraph.

```
⚡ Fast Emergency Response
"We dispatch within 30 minutes. If it's an emergency, every minute counts —
so we move like it."

💯 Upfront, Honest Pricing
"You get the full price before we touch a thing. No surprise invoices.
No 'we found something else while we were in there.'"

🏆 [YEARS_IN_BUSINESS]+ Years of Local Experience
"We've served [SERVICE_AREAS] for over [YEARS_IN_BUSINESS] years.
We know the pipes in these neighbourhoods better than anyone."
```

Each column slides in from left/right with scroll reveal.

### MidPageCTA.jsx

Full-width band, dark navy (#0f1f3d) background.
Large, centered headline: "Need a Plumber Right Now?"
Large phone button (red, pulse glow): "📞 Call [PHONE]"  href="tel:+1[PHONE_DIGITS]"
Subtext (white/50): "We answer 24/7 — evenings, weekends, holidays"

This section is intentionally simple — it should interrupt the scroll and demand action.

### Process.jsx

4-step horizontal row (desktop) / vertical timeline (mobile).
Heading: "How It Works"
Subheading: "Fast, simple, guaranteed — from first call to fixed pipes."

Steps:
  1. 📞 "Call or Text" / "Available 24/7 including holidays"
  2. ⚡ "Fast Arrival" / "We aim to be there within 60 minutes"
  3. 🔧 "Diagnose & Fix" / "Clear pricing before any work begins"
  4. ✅ "Guaranteed" / "We're not done until you're 100% happy"

Each step: number badge (navy circle) + icon + title + subtitle.
Desktop: connected by a horizontal dashed line between steps.
Mobile: connected by a vertical line on the left.
Scroll reveal: steps animate in sequence with 150ms stagger.

### Reviews.jsx

Section ID: `#reviews`
Off-white background.
Heading: "What [CITY_PROVINCE] Homeowners Are Saying"

3 review cards in a row (desktop) / stacked (mobile).
Each card:
- White background, shadow
- Gold stars (★★★★★) at top
- Quote text (italic, text-gray-700, font-medium)
- Reviewer name + "— Google Review" below
- Subtle left border (gold, 3px)
- Hover: slight lift

Use [REVIEWS_3] verbatim. Fallback if empty:
  "Showed up within the hour and fixed our burst pipe. Professional and clean."
    — Sarah M., Google Review
  "Fair pricing, no surprises. Will definitely call again."
    — David K., Google Review
  "Best plumber in [CITY_PROVINCE]. Fast, honest, and did a great job."
    — Jennifer L., Google Review

Below cards: GHL reviews placeholder div:
```jsx
<div
  id="ghl-reviews"
  style={{
    border: '2px dashed #cbd5e1',
    borderRadius: '8px',
    minHeight: '80px',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    padding: '16px',
    marginTop: '24px',
    color: '#94a3b8',
  }}
>
  Live Google Reviews load here (GHL embed)
</div>
```

Below placeholder: "Read all reviews on Google →" link styled as a text link
with an arrow icon.

### ServiceAreas.jsx

Section ID: `#service-areas`
Dark navy background — contrast break from the white sections.
Heading (white): "Proudly Serving [CITY_PROVINCE] and Surrounding Communities"

Neighborhood pills: render each area from [SERVICE_AREAS] as a pill tag:
```jsx
// White text, semi-transparent navy border, rounded-full
// Hover: white background + navy text
```

Below pills: Google Maps iFrame, 100% width, rounded corners:
```html
<iframe
  src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d92516.52!2d-79.65!3d43.59!..."
  width="100%" height="350"
  style={{ border: 0, borderRadius: '12px' }}
  loading="lazy"
  allowFullScreen
/>
```
Note: update coordinates to match [CITY_PROVINCE] if not Mississauga.

### Booking.jsx

White background.
Heading: "Schedule Non-Emergency Service"
Subtext: "For emergencies, call [PHONE] — we answer 24/7."

GHL calendar placeholder:
```jsx
<div
  id="ghl-calendar"
  style={{
    border: '2px dashed #cbd5e1',
    borderRadius: '8px',
    minHeight: '150px',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    padding: '20px',
    color: '#94a3b8',
  }}
>
  Booking Calendar loads here (GHL embed)
</div>
```

### Contact.jsx

Section ID: `#contact`
Off-white background. 2-column layout (desktop): form left, contact info right.

GHL contact form placeholder (left):
```jsx
<div
  id="ghl-contact-form"
  style={{
    border: '2px dashed #cbd5e1',
    borderRadius: '8px',
    minHeight: '200px',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    padding: '20px',
    color: '#94a3b8',
  }}
>
  Contact Form loads here (GHL embed)
</div>
```

Right column (contact info):
- 📞 [PHONE] — large, red, tap-to-call link
- "Licensed & Insured"
- "Free Estimates"
- "Available 24/7"
- Response promise: "We respond within 2 hours. Emergencies answered immediately."

### Footer.jsx

Dark navy (#0f1f3d) background, white text. 4 columns (desktop) / 2 columns (tablet) / stacked (mobile).

Column 1: Business name + short tagline
Column 2: Contact (phone tap-to-call, city, email if available)
Column 3: Top 5-6 service areas from [SERVICE_AREAS]
Column 4: Hours (24/7 emergency line)

Bottom bar:
- Left: © {new Date().getFullYear()} [BUSINESS_NAME]. All rights reserved.
- Right: GHL pay invoice button:
  ```jsx
  <a
    href="#"
    id="ghl-payment-link"
    style={{
      display: 'inline-block',
      background: 'rgba(255,255,255,0.1)',
      color: 'white',
      padding: '8px 20px',
      borderRadius: '6px',
      textDecoration: 'none',
      fontSize: '13px',
      border: '1px solid rgba(255,255,255,0.2)',
    }}
  >
    Pay Invoice
  </a>
  ```

### App.jsx

Import and render all components in section order:
```jsx
<Header />
<Hero />
<VoiceWidgetPlaceholder />
<TrustBar />
<Services />
<WhyChooseUs />
<MidPageCTA />
<Process />
<Reviews />
<ServiceAreas />
<Booking />
<Contact />
<Footer />
```

### public/favicon.svg

A simple plumbing wrench icon in navy:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" fill="#1a3a6b">
  <path d="M28.5 4.5a1 1 0 00-1.4 0l-6.6 6.6-2.8-2.8 6.6-6.6a1 1 0 000-1.4
    C22.6-.9 19.5.2 17.8 2.5a7 7 0 00.8 9.2l-12 12a3 3 0 004.2 4.2l12-12
    a7 7 0 009.2.8c2.3-1.7 3.4-4.8 2.2-7.1a1 1 0 00-1.4-.1l-4.2 4.2-2.1-2.1
    4.2-4.2a1 1 0 000-1.4z"/>
</svg>
```

---

## TECHNICAL REQUIREMENTS

```
✅ React 18 + Vite + Tailwind CSS
✅ vite.config.js: build.outDir explicitly set to 'dist'
✅ Mobile-first: 390px baseline → md: → lg:
✅ All phone numbers: href="tel:+1[PHONE_DIGITS]" — no plain text phone numbers
✅ Dynamic copyright: {new Date().getFullYear()}
✅ Semantic HTML: h1, h2, section, article, nav (SEO-indexable)
✅ No video backgrounds (performance)
✅ No Lorem Ipsum in final output
✅ No builder badges or watermarks
✅ prefers-reduced-motion respected
✅ All 6 GHL placeholder divs present (see index.html + component specs above)
✅ GHL inline widget: static HTML outside React root in index.html
✅ GHL floating widget: div + comment before </body> in index.html
✅ useScrollReveal hook applied to all major sections
✅ CSS keyframes defined in src/index.css
✅ IntersectionObserver: threshold 0.12, fires once (unobserve after trigger)
✅ Stagger animations in grids: 100ms delay per item
✅ Parallax on hero background: 0.3x scroll speed
✅ Glassmorphism on trust bar: backdrop-blur-md + semi-transparent border
✅ Emergency CTA button: pulseGlow animation, stops on hover
```

---

## GHL PLACEHOLDER DIVS — COMPLETE REFERENCE

All 6 must be present. IDs are exact — do not rename.

```
id="ghl-voice-inline"   → in index.html, outside <div id="root"> (static HTML)
id="ghl-calendar"       → in Booking.jsx
id="ghl-contact-form"   → in Contact.jsx
id="ghl-reviews"        → in Reviews.jsx
id="ghl-payment-link"   → in Footer.jsx (as an <a> tag)
id="ghl-chat-widget"    → in index.html, before </body> (static HTML)
```

---

## PACKAGE.JSON

```json
{
  "name": "plumbing-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14",
    "vite": "^5.4.10"
  }
}
```

---

## DEPLOY INSTRUCTIONS (output at end of response)

After Gemini outputs all files:

```bash
# 1. Create project folder and copy all files into it

# 2. Install dependencies
npm install

# 3. Build
npm run build
# Outputs to: dist/  (set explicitly in vite.config.js)

# 4. Deploy to Netlify
netlify deploy --dir=dist --prod
# Returns a live URL like: https://your-site.netlify.app

# 5. Share the URL in cold outreach
```

---

## CONVERTER PROMPT (paste after site is complete, for single-file HTML demo)

After Gemini outputs the full multi-file project, paste this as a follow-up
to get a single self-contained HTML file for quick demos or GHL Custom HTML embed:

```
CONVERTER TASK:

Rewrite the complete React/Vite app you just generated as a single,
self-contained vanilla HTML file.

Rules:
- ALL CSS inlined in a <style> block in <head>
- ALL JavaScript inlined in a <script> block before </body>
- ALL React JSX converted to vanilla JS (no React library required)
- CSS animations and keyframes preserved exactly
- IntersectionObserver scroll reveals preserved
- All 6 GHL placeholder divs preserved with exact same IDs
- All href="tel:+1[PHONE_DIGITS]" links preserved
- Dynamic copyright year: new Date().getFullYear()
- No external dependencies — zero CDN links
- No build step required — file opens directly in browser

Output: a single complete HTML file, nothing else.
Do not summarize. Do not truncate. Output the entire file.
```

This single file can be:
- Dropped into netlify.com/drop for instant demo hosting
- Pasted into GHL's Custom HTML element
- Sent as an email attachment for async demos

================================================================================
VARIABLE REFERENCE
================================================================================
[BUSINESS_NAME]    → e.g., Mississauga Plumbing Services Ltd.
[PHONE]            → e.g., (647) 550-4003
[PHONE_DIGITS]     → e.g., 16475504003  (digits only, with country code 1)
[CITY_PROVINCE]    → e.g., Mississauga, ON
[SERVICES_LIST]    → e.g., Emergency Plumbing, Drain Cleaning, Water Heater...
[SERVICE_AREAS]    → e.g., Mississauga, Brampton, Oakville, Port Credit...
[YEARS_IN_BUSINESS]→ e.g., 15+
[RATING_STRING]    → e.g., 4.9/5 · 166 Google Reviews
[REVIEWS_3]        → 3 verbatim review texts from GBP, one per line
================================================================================
